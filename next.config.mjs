/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['m.media-amazon.com'], // Add the movie poster domain here
    },
  };
  
  export default nextConfig;
  